<div class="p-2 d-flex flex-grow-1 justify-content-center align-items-center">
<div class="w-50 card mb-3" style="cursor: pointer; height: 200px" onclick="this.closest('form').submit()">
    <div class="h-100 row g-0">
        <div class="col-md-8">
            <div class="card-body">
                <p class="card-title">Name: <%= user.name %> <%= user.surname %></p>
                <p class="card-text">Email: <%= user.email %></p>
                <p class="card-text">ID:    <%= user.id %></p>
            </div>
        </div>
        <div class="d-flex flew-grow-2">
            <form action="TODO" method="get" class="p-2 ps-4">
                <button class="btn btn-primary">Update</button>
            </form>
            <form action="TODO" method="get" class="p-2">
                <button class="btn btn-primary">Delete</button>
            </form>
        </div>
    </div>
</div>
</div>